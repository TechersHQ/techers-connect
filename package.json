{
  "name": "techersng",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build src/index.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/ibrahimraimi/techers-ng"
  },
  "keywords": [],
  "author": "Ibrahim Raimi",
  "bugs": {
    "url": "https://github.com/ibrahimraimi/techers-ng"
  },
  "devDependencies": {
    "parcel": "2.7.0"
  },
  "dependencies": {
    "gsap": "3.11.1"
  }
}
